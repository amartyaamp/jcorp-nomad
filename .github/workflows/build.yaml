name: Build ESP32-S3 Firmware

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Install Arduino CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          echo "$PWD/bin" >> $GITHUB_PATH
      
      - name: Verify Arduino CLI version
        run: arduino-cli version

      - name: Install ESP32 Core
        run: |
          arduino-cli core update-index
          arduino-cli core install esp32:esp32@3.3.0

      - name: Install required libraries
        run: |
          arduino-cli lib update-index
          arduino-cli lib install ArduinoJson@7.3.0
          arduino-cli lib install lvgl@8.3.10
          arduino-cli lib install AsyncTCP

      # - name: Download ESPAsyncWebServer 1.3.0 and add to local lib
      #   run: |
      #     curl -L https://github.com/esphome/ESPAsyncWebServer/archive/refs/tags/v1.3.0.tar.gz | tar -xz
      #     mkdir -p lib
      #     mv ESPAsyncWebServer-1.3.0 lib/ESPAsyncWebServer

      - name: Compile firmware
        run: |
          mkdir -p firmware/JcorpNomadProject/build
          arduino-cli compile \
            --fqbn esp32:esp32:esp32s3:PSRAM=opi,CDCOnBoot=cdc,PartitionScheme=app3M_fat9M_16MB \
            --build-property build.panic=print \
            --output-dir $GITHUB_WORKSPACE/firmware/JcorpNomadProject/build \
            --libraries $GITHUB_WORKSPACE/lib \
            $GITHUB_WORKSPACE/firmware/JcorpNomadProject/JcorpNomadProject.ino

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: jcorp-nomad-firmware
          path: firmware/JcorpNomadProject/build/JcorpNomadProject.ino.bin
          if-no-files-found: error
          compression-level: 6